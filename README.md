这是【 KANE face-to-face app】用于拉取 huggingface 服务器地址列表的 json文件，【位于huggingface上的服务器帮你获取微软TTS，我有部署多个服务器，每个服务器的地址列在这里，供APP拉取，APP也开源了，但是暗号需要单独分发。】

用户也可以去huggingface自己部署用于获取微软TTS的服务，而不完全依赖我这两个位于huggingface上的服务器。
